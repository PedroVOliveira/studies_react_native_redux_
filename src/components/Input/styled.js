import {css} from 'styled-components/native';
import {DEFAULT_PADDING} from '../../../setting';
export const base = css`
  border-bottom-color:black;
  border-bottom-width:2px;
  width:90%;
  font-size:16px;
  padding-left:10px;
  padding-bottom:10px;
  margin-bottom:${DEFAULT_PADDING};

`;
